import React, { Fragment } from "react";

const DelivePayPage = () => {
    return React.createElement(
        Fragment,
        null,
        React.createElement(
            "p",
            null,
            "DelivePayPage"
        )
    );
};

export default DelivePayPage;