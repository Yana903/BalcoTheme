import React, { Fragment } from "react";

const NewsPage = () => {
    return React.createElement(
        Fragment,
        null,
        React.createElement(
            "p",
            null,
            "NewsPage"
        )
    );
};

export default NewsPage;