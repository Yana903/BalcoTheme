import React, { Fragment } from "react";

const AboutPage = () => {
    return React.createElement(
        Fragment,
        null,
        React.createElement(
            "p",
            null,
            "AboutPage"
        )
    );
};

export default AboutPage;